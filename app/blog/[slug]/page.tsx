import { posts } from '@/lib/data';import { notFound } from 'next/navigation';import { Section } from '@/components/ui';export default function({params}:{params:{slug:string}}){const p=posts.find(x=>x.slug===params.slug);if(!p) return notFound();return <main><Section title={p.title} kicker='ARTICLE'><p className='text-white/75'>Actionable guidance for gym owners: optimize speed, sharpen offer messaging, add social proof, and simplify booking journeys to raise conversion rates.</p></Section></main>}
